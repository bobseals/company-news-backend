# package.json - Copy this EXACTLY
{
  "name": "company-news-backend",
  "version": "1.0.0",
  "description": "Backend API for company news and financial data",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build needed for Node.js'",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": "18.x"
  },
  "keywords": ["api", "news", "finance", "company"],
  "author": "Your Name",
  "license": "ISC"
}

# Vercel Settings to Use:
Build Command: npm install
Output Directory: (leave blank)
Install Command: npm install
Development Command: npm run dev

# OR try these alternative settings:
Build Command: (leave blank)
Output Directory: (leave blank) 
Install Command: npm ci
Development Command: npm startecho \"Error: no test specified\" && exit 1"
  }
}

# .gitignore file (create this to avoid committing secrets)
node_modules/
.env
.DS_Store
*.log